#include <stdio.h>
#include <math.h>

// emits a binary symbol with P(X=0) = p
unsigned char emit_source_symbol(double p);

unsigned char emit_source_symbol(double p){

}

int main(int argc, char *argv[]){
  return 0;
}